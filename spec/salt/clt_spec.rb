require 'spec_helper'

describe Salt::CLT do
  it 'has a version number' do
    expect(Salt::CLT::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
